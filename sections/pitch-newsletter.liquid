<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<section class="section section--{{ section.settings.section_width }} pitch-newsletter color-{{ section.settings.color_scheme }}">
  <div class="pitch-newsletter__inner">
    {% if section.settings.heading != blank %}
      <h2 class="pitch-newsletter__title">{{ section.settings.heading }}</h2>
    {% endif %}
    {% if section.settings.subheading != blank %}
      <p class="pitch-newsletter__subtitle">{{ section.settings.subheading }}</p>
    {% endif %}
    {% form 'customer', class: 'pitch-newsletter__form' %}
      <input type="hidden" name="contact[tags]" value="newsletter">
      <input class="pitch-newsletter__input" type="email" name="contact[email]" placeholder="Email address" required>
      <button class="pitch-newsletter__button" type="submit">{{ section.settings.button_label }}</button>
    {% endform %}
  </div>
</section>

{% schema %}
{
  "name": "Pitch Newsletter",
  "class": "section-wrapper",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "JOIN THE SQUAD" },
    { "type": "text", "id": "subheading", "label": "Subheading", "default": "Get early access to new posters and exclusive drops." },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "SUBSCRIBE" },
    { "type": "select", "id": "section_width", "label": "Section width", "default": "full-width", "options": [
      { "value": "page-width", "label": "Page" },
      { "value": "full-width", "label": "Full" }
    ]},
    { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-4" }
  ],
  "presets": [
    { "name": "Pitch Newsletter" }
  ]
}
{% endschema %}
